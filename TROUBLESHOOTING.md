# 🔧 วิธีแก้ปัญหาข้อมูลหายหลังรีเฟรช

## ✅ ปัญหาที่แก้ไขแล้ว

เพิ่งแก้ไขปัญหาสำคัญที่ทำให้ข้อมูลไม่ถูกบันทึกลง Firebase:

### ปัญหาเดิม:
- `isInitialLoad.current` ถูกตั้งเป็น `false` **หลังจาก** dispatch `updateLoginStreak`
- ทำให้การ dispatch login streak ไม่ถูกบันทึกลง Firestore
- ข้อมูลที่เพิ่มหลังจาก login อาจไม่ถูกบันทึก

### การแก้ไข:
✅ ย้าย `isInitialLoad.current = false` ไปไว้**ก่อน** dispatch `updateLoginStreak`
✅ เพิ่ม console.log เพื่อ debug การบันทึก/โหลดข้อมูล
✅ เพิ่ม error handling ที่ดีขึ้น

---

## 🔍 วิธีตรวจสอบว่าข้อมูลถูกบันทึกหรือไม่

### 1. เปิด Browser Console (F12)
เมื่อคุณเพิ่มวิชาหรือแก้ไขข้อมูล คุณจะเห็น log แบบนี้:

```
💾 กำลังบันทึกข้อมูลลง Firestore... {subjects: [...], courses: [...], ...}
✅ บันทึกข้อมูลสำเร็จ!
```

### 2. ตรวจสอบใน Firebase Console
1. ไปที่ https://console.firebase.google.com/
2. เลือกโปรเจค `me-u-5ab65`
3. ไปที่ Firestore Database
4. ดูใน collection `schedules` → document ที่เป็น userId ของคุณ
5. ตรวจสอบว่ามี field `courses` และ `subjects` หรือไม่

### 3. ทดสอบการรีเฟรช
1. เพิ่มวิชาใหม่
2. รอให้เห็น "✅ บันทึกข้อมูลสำเร็จ!" ใน console
3. กด F5 รีเฟรชหน้า
4. ตรวจสอบว่าวิชายังอยู่หรือไม่

---

## ⚠️ ถ้าข้อมูลยังหายอยู่

### กรณีที่ 1: เห็น "⏸️ ข้ามการบันทึก" ใน console
**สาเหตุ:** isInitialLoad ยังเป็น true
**วิธีแก้:** รอสักครู่หลัง login แล้วค่อยเพิ่มข้อมูล

### กรณีที่ 2: เห็น "❌ Error writing document"
**สาเหตุ:** Firestore rules ไม่อนุญาต หรือไม่มีอินเทอร์เน็ต
**วิธีแก้:** 
1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
2. ตรวจสอบ Firestore Rules ใน Firebase Console:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /schedules/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### กรณีที่ 3: ไม่เห็น log อะไรเลย
**สาเหตุ:** Firebase ไม่ได้เชื่อมต่อ
**วิธีแก้:**
1. ตรวจสอบไฟล์ `src/firebase.js`
2. ตรวจสอบว่า Firebase config ถูกต้อง
3. ลองล็อกเอาท์แล้วล็อกอินใหม่

---

## 🎯 Tips สำหรับการใช้งาน

1. **รอให้โหลดข้อมูลเสร็จก่อน** - หลัง login รอให้เห็น "📥 โหลดข้อมูลจาก Firestore" ก่อนเพิ่มข้อมูล
2. **ตรวจสอบ console เสมอ** - เปิด console (F12) เพื่อดู log การบันทึก
3. **ใช้ Export/Import** - ใช้ฟีเจอร์ Export เป็น JSON เป็นสำรอง (ในหน้า Settings)

---

## 📝 หมายเหตุ

- ข้อมูลจะถูกบันทึกอัตโนมัติทุกครั้งที่มีการเปลี่ยนแปลง
- ใช้ `onSnapshot` เพื่อ sync แบบ real-time
- ข้อมูลจะถูก merge กับข้อมูลเดิมใน Firestore
