# 📤 คู่มือการแชร์ตารางเรียน (Public Schedule Share)

## 🎯 สิ่งที่ต้องทำ

ฉันได้สร้างระบบแชร์ตารางเรียนให้แล้ว แต่ยังต้องเพิ่มโค้ดเล็กน้อยใน `App.jsx`

---

## ✅ ไฟล์ที่สร้างแล้ว

1. ✅ `src/PublicScheduleView.jsx` - หน้าแสดงตารางเรียนแบบ read-only

---

## 📝 ขั้นตอนการติดตั้ง

### ขั้นที่ 1: เพิ่มโค้ดใน App.jsx

เปิดไฟล์ `src/App.jsx` แล้วเพิ่มโค้ดตามนี้:

#### 1.1 หาบรรทัดที่มี `useEffect(()=>{ tasks.forEach(scheduleReminder) }, [tasks])`

ประมาณบรรทัด 375

#### 1.2 เพิ่มโค้ดนี้ **ก่อน** บรรทัด `if (loadingAuth) {`

```javascript
  // ========== ถ้าเป็นโหมดแชร์ตารางเรียน (Public View) ==========
  if (isPublicView) {
    if (!sharedUserData) {
      return (
        <div className="h-screen flex flex-col items-center justify-center bg-slate-100 dark:bg-slate-900 p-4">
          <div className="text-center">
            <div className="text-6xl mb-4">📅</div>
            <h1 className="text-2xl font-bold mb-2">กำลังโหลดตารางเรียน...</h1>
            <p className="text-slate-500">รอสักครู่นะ</p>
          </div>
        </div>
      );
    }

    return <PublicScheduleView data={sharedUserData} />;
  }
```

---

### ขั้นที่ 2: แก้ไขปุ่มแชร์

หาส่วนปุ่มแชร์ในไฟล์ `App.jsx` (ประมาณบรรทัด 870-880)

#### เปลี่ยนจาก:
```javascript
<Button onClick={() => {
  if (!userId) {
    alert('ไม่สามารถแชร์ได้ กรุณาเข้าสู่ระบบก่อน');
    return;
  }
  const shareUrl = `${window.location.origin}/share/${userId}`;
  navigator.clipboard.writeText(shareUrl).then(() => {
    alert(`คัดลอกลิงก์สำหรับแชร์แล้ว!\n${shareUrl}`);
  }).catch(() => {
    alert('ไม่สามารถคัดลอกลิงก์ได้');
  });
}}>
```

#### เป็น:
```javascript
<Button onClick={() => {
  if (!userId) {
    alert('ไม่สามารถแชร์ได้ กรุณาเข้าสู่ระบบก่อน');
    return;
  }
  const shareUrl = `${window.location.origin}?share=${userId}`;
  navigator.clipboard.writeText(shareUrl).then(() => {
    alert(`คัดลอกลิงก์สำหรับแชร์แล้ว!\n${shareUrl}\n\nเพื่อนของคุณสามารถเปิดลิงก์นี้เพื่อดูตารางเรียนได้โดยไม่ต้องล็อกอิน`);
  }).catch(() => {
    alert('ไม่สามารถคัดลอกลิงก์ได้');
  });
}}>
```

**สิ่งที่เปลี่ยน:**
- จาก `/share/${userId}` → `?share=${userId}`
- เพิ่มข้อความอธิบายในการแชร์

---

## 🚀 วิธีใช้งาน

### สำหรับผู้แชร์ (คุณ):

1. เข้าหน้า **ตารางเรียน**
2. กดปุ่ม **แชร์ตารางเรียน**
3. ลิงก์จะถูกคัดลอกอัตโนมัติ
4. ส่งลิงก์ให้เพื่อน

**ตัวอย่างลิงก์:**
```
https://your-site.com/?share=VMO2JIHHSzXLIJDPMAv83XhNazs2
```

### สำหรับผู้รับลิงก์ (เพื่อน):

1. เปิดลิงก์ที่ได้รับ
2. จะเห็นหน้าตารางเรียนแบบ **read-only**
3. สามารถ:
   - ✅ ดูตารางเรียนวันนี้
   - ✅ เลื่อนดูวันอื่นๆ (จันทร์-ศุกร์)
   - ✅ เห็นสัปดาห์คู่/คี่
   - ✅ เห็นสถานะออนไลน์/ออนไซต์
4. **ไม่สามารถ**:
   - ❌ แก้ไขตารางเรียน
   - ❌ เพิ่ม/ลบวิชา
   - ❌ เปลี่ยนสถานะ

---

## 🎨 ฟีเจอร์ของหน้า Public View

### 1. **แสดงตารางเรียนประจำวัน**
- แสดงวิชาที่เรียนในวันที่เลือก
- แสดงสถานะ ออนไลน์/ออนไซต์
- แสดงเวลาเรียน, ห้องเรียน, อาจารย์

### 2. **เลื่อนดูวันอื่นๆ**
- ปุ่มวันก่อนหน้า/วันถัดไป
- ข้ามวันเสาร์-อาทิตย์อัตโนมัติ

### 3. **แสดงสัปดาห์คู่/คี่**
- คำนวณสัปดาห์คู่/คี่อัตโนมัติ
- แสดงสถานะที่ถูกต้องตามสัปดาห์

### 4. **ดีไซน์สวยงาม**
- ใช้สีของวิชาแต่ละวิชา
- Responsive (ใช้ได้ทั้งมือถือและคอมพิวเตอร์)
- Dark mode support

---

## 🔒 ความปลอดภัย

### ✅ ปลอดภัย:
- ดึงข้อมูลจาก Firestore แบบ read-only
- ไม่สามารถแก้ไขข้อมูลได้
- ไม่ต้องล็อกอิน

### ⚠️ ข้อควรระวัง:
- **ทุกคนที่มีลิงก์สามารถดูตารางเรียนได้**
- ถ้าไม่ต้องการให้ใครดู → **อย่าแชร์ลิงก์**
- ข้อมูลที่แสดง: วิชาเรียน, เวลา, ห้อง, อาจารย์
- **ไม่แสดง**: งาน (tasks), การบ้าน

---

## 🛠️ Firestore Rules

ตรวจสอบว่า Firestore Rules อนุญาตให้อ่านข้อมูลได้:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /schedules/{userId} {
      // อนุญาตให้เจ้าของแก้ไขได้
      allow write: if request.auth != null && request.auth.uid == userId;
      
      // อนุญาตให้ทุกคนอ่านได้ (สำหรับการแชร์)
      allow read: if true;
    }
  }
}
```

**หมายเหตุ:** ถ้าคุณต้องการให้เฉพาะคนที่ล็อกอินเท่านั้นที่ดูได้ เปลี่ยน `allow read: if true;` เป็น `allow read: if request.auth != null;`

---

## 🎯 ตัวอย่างการใช้งาน

### Scenario 1: แชร์ให้เพื่อนดูตารางเรียน
```
คุณ: กดปุ่มแชร์ → ได้ลิงก์
คุณ: ส่งลิงก์ให้เพื่อนทาง Line/Facebook
เพื่อน: เปิดลิงก์ → เห็นตารางเรียนของคุณ
เพื่อน: เลื่อนดูวันอื่นๆ ได้
```

### Scenario 2: แชร์ให้ครอบครัวดูว่าวันไหนเรียน
```
คุณ: กดปุ่มแชร์ → ได้ลิงก์
คุณ: ส่งให้พ่อแม่
พ่อแม่: เปิดลิงก์ → รู้ว่าวันไหนเรียนออนไซต์/ออนไลน์
```

---

## 💡 Tips

1. **บุ๊กมาร์กลิงก์** - เพื่อนสามารถบุ๊กมาร์กลิงก์ไว้ดูได้ตลอด
2. **ข้อมูล Real-time** - ถ้าคุณแก้ไขตารางเรียน เพื่อนจะเห็นการเปลี่ยนแปลงทันที
3. **ไม่มีวันหมดอายุ** - ลิงก์ใช้ได้ตลอดจนกว่าคุณจะลบบัญชี

---

## 🆘 แก้ปัญหา

### ปัญหา: เพื่อนเปิดลิงก์แล้วไม่เห็นตารางเรียน

**วิธีแก้:**
1. ตรวจสอบว่าคุณมีวิชาเรียนในระบบหรือยัง
2. ตรวจสอบ Firestore Rules (ต้องอนุญาตให้ read)
3. ลองรีเฟรชหน้าเว็บ

### ปัญหา: ลิงก์แชร์ไม่ทำงาน

**วิธีแก้:**
1. ตรวจสอบว่าเพิ่มโค้ดใน App.jsx ครบหรือยัง
2. ตรวจสอบ Console (F12) ดู error
3. ตรวจสอบว่า import PublicScheduleView แล้วหรือยัง

---

## 📚 สรุป

ระบบแชร์ตารางเรียนนี้จะทำให้:
- ✅ เพื่อนดูตารางเรียนของคุณได้โดยไม่ต้องล็อกอิน
- ✅ แสดงเฉพาะตารางเรียน (read-only)
- ✅ ข้อมูล real-time
- ✅ ใช้งานง่าย แค่แชร์ลิงก์

**พร้อมใช้งานแล้ว!** 🎉
