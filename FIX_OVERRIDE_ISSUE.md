# 🔧 แก้ปัญหา: สถานะชั่วคราวหายหลังรีเฟรช

## 🔴 ปัญหา
หลังจากกดเปลี่ยนสถานะชั่วคราว (ออนไลน์/ออนไซต์) แล้วรีเฟรชหน้าเว็บ สถานะกลับมาเป็นแบบเดิม

## 🔍 สาเหตุ
`scheduleOverrides` ถูกบันทึกลง Firestore แล้ว แต่อาจมีปัญหาในการโหลดกลับมา

## ✅ วิธีแก้ไข

### วิธีที่ 1: ตรวจสอบ Console Log

1. เปิด Browser Console (กด F12)
2. กดเปลี่ยนสถานะชั่วคราว
3. ดูว่ามี log อะไรบ้าง:
   - ควรเห็น "💾 กำลังบันทึกข้อมูลลง Firestore..."
   - ควรเห็น "✅ บันทึกข้อมูลสำเร็จ!"
4. รีเฟรชหน้าเว็บ
5. ดูว่ามี log อะไรบ้าง:
   - ควรเห็น "📥 โหลดข้อมูลจาก Firestore:"
   - ตรวจสอบว่ามี `scheduleOverrides` ในข้อมูลที่โหลดหรือไม่

### วิธีที่ 2: ตรวจสอบ Firestore

1. ไปที่ https://console.firebase.google.com/
2. เลือกโปรเจค `me-u-5ab65`
3. ไปที่ Firestore Database
4. เปิด collection `schedules`
5. เปิด document ที่เป็น userId ของคุณ
6. ตรวจสอบว่ามี field `scheduleOverrides` หรือไม่
7. ถ้ามี ดูว่าข้อมูลถูกต้องหรือไม่ (ควรเป็น object ที่มี key เป็น `courseId_weekStartDate`)

### วิธีที่ 3: เพิ่ม Logging (แนะนำ)

เปิดไฟล์ `src/App.jsx` แล้วหาบรรทัดประมาณ 193:

```javascript
console.log("💾 กำลังบันทึกข้อมูลลง Firestore...", cleanState);
```

**เปลี่ยนเป็น:**

```javascript
console.log("💾 กำลังบันทึกข้อมูลลง Firestore...");
console.log("📊 scheduleOverrides:", state.scheduleOverrides);
console.log("📦 cleanState:", cleanState);
```

แล้วที่บรรทัด 197:

```javascript
console.log("✅ บันทึกข้อมูลสำเร็จ!");
```

**เปลี่ยนเป็น:**

```javascript
console.log("✅ บันทึกข้อมูลสำเร็จ!");
console.log("✅ scheduleOverrides ที่บันทึก:", cleanState.scheduleOverrides);
```

### วิธีที่ 4: ตรวจสอบการโหลดข้อมูล

เปิดไฟล์ `src/App.jsx` แล้วหาบรรทัดประมาณ 148:

```javascript
console.log("📥 โหลดข้อมูลจาก Firestore:", data);
```

**เพิ่มบรรทัดนี้ด้านล่าง:**

```javascript
console.log("📥 โหลดข้อมูลจาก Firestore:", data);
console.log("🔄 scheduleOverrides ที่โหลด:", data.scheduleOverrides);
```

---

## 🧪 การทดสอบ

1. **ล้าง Console** (คลิกขวาใน Console → Clear console)
2. **กดเปลี่ยนสถานะชั่วคราว**
3. **ดู Console** - ควรเห็น:
   ```
   💾 กำลังบันทึกข้อมูลลง Firestore...
   📊 scheduleOverrides: { "courseId_2025-12-02": "online" }
   ✅ บันทึกข้อมูลสำเร็จ!
   ```
4. **รีเฟรชหน้าเว็บ (F5)**
5. **ดู Console** - ควรเห็น:
   ```
   📥 โหลดข้อมูลจาก Firestore: { ..., scheduleOverrides: { ... } }
   🔄 scheduleOverrides ที่โหลด: { "courseId_2025-12-02": "online" }
   ```

---

## 🎯 ถ้ายังไม่ได้

### กรณีที่ 1: ไม่เห็น scheduleOverrides ใน Firestore

**สาเหตุ:** ข้อมูลไม่ถูกบันทึก

**วิธีแก้:**
1. ตรวจสอบว่ามี error ใน Console หรือไม่
2. ตรวจสอบ Firestore Rules:
   ```javascript
   allow write: if request.auth != null && request.auth.uid == userId;
   ```

### กรณีที่ 2: มี scheduleOverrides ใน Firestore แต่โหลดไม่ได้

**สาเหตุ:** reducer ไม่ load scheduleOverrides

**วิธีแก้:**
เปิดไฟล์ `src/App.jsx` หาบรรทัดประมาณ 28-42 (reducer function):

ตรวจสอบว่ามีบรรทัดนี้หรือไม่:
```javascript
case 'load': {
  const loaded = action.payload;
  if (typeof loaded !== 'object' || loaded === null) {
    return initialState;
  }
  return {
    ...initialState,
    subjects: Array.isArray(loaded.subjects) ? loaded.subjects.filter(s => s && typeof s === 'object') : [],
    tasks: Array.isArray(loaded.tasks) ? loaded.tasks.filter(t => t && typeof t === 'object') : [],
    courses: Array.isArray(loaded.courses) ? loaded.courses.filter(c => c && typeof c === 'object') : [],
    scheduleOverrides: typeof loaded.scheduleOverrides === 'object' && loaded.scheduleOverrides !== null ? loaded.scheduleOverrides : {},
    lastLogin: loaded.lastLogin || null,
    loginStreak: loaded.loginStreak || 0,
  };
}
```

**ตรวจสอบว่ามีบรรทัดนี้:**
```javascript
scheduleOverrides: typeof loaded.scheduleOverrides === 'object' && loaded.scheduleOverrides !== null ? loaded.scheduleOverrides : {},
```

ถ้าไม่มี ให้เพิ่มเข้าไป!

### กรณีที่ 3: โหลดได้แต่ไม่แสดงผล

**สาเหตุ:** getCourseStatus ไม่ใช้ scheduleOverrides

**วิธีแก้:**
ตรวจสอบว่าเมื่อเรียก `getCourseStatus` ส่ง `state.scheduleOverrides` เข้าไปด้วยหรือไม่

ตัวอย่าง:
```javascript
const { status, isOverridden } = getCourseStatus(course, selectedDate, state.scheduleOverrides);
```

---

## 📝 สรุป

ปัญหาน่าจะเกิดจาก:
1. ❌ ข้อมูลไม่ถูกบันทึกลง Firestore
2. ❌ ข้อมูลถูกบันทึกแต่โหลดไม่ได้
3. ❌ ข้อมูลโหลดได้แต่ไม่ถูกใช้

ใช้วิธีด้านบนเพื่อหาว่าปัญหาอยู่ตรงไหน แล้วแก้ไขตามนั้น

---

## 🆘 ถ้ายังแก้ไม่ได้

ส่ง screenshot ของ:
1. Console log หลังกดเปลี่ยนสถานะ
2. Console log หลังรีเฟรช
3. Firestore Database (schedules collection)

แล้วฉันจะช่วยแก้ไขให้!
