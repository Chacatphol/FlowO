# 🎨 คู่มือปรับแต่งหน้าตาตารางเรียน

## 📋 สารบัญ
1. [ตารางรายสัปดาห์](#ตารางรายสัปดาห์)
2. [สีและธีม](#สีและธีม)
3. [ขนาดและระยะห่าง](#ขนาดและระยะห่าง)
4. [เอฟเฟกต์และ Animation](#เอฟเฟกต์และ-animation)

---

## 🗓️ ตารางรายสัปดาห์

### 📍 ตำแหน่งในโค้ด
ไฟล์: `src/App.jsx` บรรทัดประมาณ 947-1038

### 🎯 ส่วนประกอบหลัก

#### 1. **ส่วนหัวตาราง (Header)**
```javascript
// บรรทัด 957
<th className="border border-slate-200 p-2 bg-slate-50 text-sm font-semibold min-w-[140px]">
```

**ปรับแต่งได้:**
- `min-w-[140px]` = ความกว้างขั้นต่ำของคอลัมน์ (เปลี่ยนเป็น 160px, 180px ได้)
- `text-sm` = ขนาดตัวอักษร (เปลี่ยนเป็น `text-base`, `text-lg` ได้)
- `bg-slate-50` = สีพื้นหลัง (เปลี่ยนเป็น `bg-blue-50`, `bg-indigo-50` ได้)
- `p-2` = padding (เปลี่ยนเป็น `p-3`, `p-4` เพื่อเพิ่มพื้นที่)

#### 2. **คอลัมน์เวลา (ซ้ายสุด)**
```javascript
// บรรทัด 972
<td className="border p-2 text-xs bg-slate-50 sticky left-0 z-10">
```

**ปรับแต่งได้:**
- `sticky left-0` = ติดด้านซ้ายเสมอ (ลบออกถ้าไม่ต้องการ)
- `bg-slate-50` = สีพื้นหลัง
- `text-xs` = ขนาดตัวอักษร

#### 3. **ช่วงเวลาที่แสดง**
```javascript
// บรรทัด 967
['08:00', '09:00', '10:00', '11:00', '12:00', '13:00', '14:00', '15:00', '16:00', '17:00']
```

**ปรับแต่งได้:**
- เพิ่มเวลา: `'18:00', '19:00', '20:00'`
- ลดเวลา: ลบออก
- เปลี่ยนช่วง: `'07:00'` เริ่มเช้ากว่า

---

## 🎨 สีและธีม

### 🔵 สีแถบสถานะ (ออนไลน์/ออนไซต์)

#### ออนไลน์ (น้ำเงิน)
```javascript
// บรรทัด 987
const statusBgColor = status === 'online' ? 'bg-blue-500' : ...
```

**เปลี่ยนสีได้:**
- `bg-blue-400` = น้ำเงินอ่อนกว่า
- `bg-blue-600` = น้ำเงินเข้มกว่า
- `bg-cyan-500` = ฟ้า
- `bg-purple-500` = ม่วง

#### ออนไซต์ (เขียว)
```javascript
'bg-green-500'
```

**เปลี่ยนสีได้:**
- `bg-green-400` = เขียวอ่อนกว่า
- `bg-green-600` = เขียวเข้มกว่า
- `bg-emerald-500` = เขียวมรกต
- `bg-teal-500` = เขียวอมฟ้า

### 🌈 สีพื้นหลัง Cell วิชา

```javascript
// บรรทัด 1001
style={{ backgroundColor: hexToRgba(courseAtTime.color, 0.15) }}
```

**ปรับความโปร่งแสง:**
- `0.15` = โปร่งแสง 15% (ค่าปัจจุบัน)
- `0.10` = โปร่งแสงมากขึ้น (จางกว่า)
- `0.25` = โปร่งแสงน้อยลง (เข้มกว่า)
- `0.30` = เข้มมาก

---

## 📏 ขนาดและระยะห่าง

### 📝 ขนาดตัวอักษร

#### ชื่อวิชา
```javascript
// บรรทัด 1016
<div className="font-medium text-xs">
```

**เปลี่ยนขนาด:**
- `text-xs` = เล็กมาก (ปัจจุบัน)
- `text-sm` = เล็ก
- `text-base` = ปกติ
- `text-lg` = ใหญ่

#### รหัสวิชา
```javascript
// บรรทัด 1017
<div className="text-[10px]">
```

**เปลี่ยนขนาด:**
- `text-[10px]` = 10 พิกเซล (ปัจจุบัน)
- `text-[11px]` = 11 พิกเซล
- `text-xs` = ขนาดมาตรฐานเล็ก

### 📦 Padding (ระยะห่างภายใน)

#### Cell วิชา
```javascript
// บรรทัด 1015
<div className="p-2 flex-1">
```

**ปรับระยะห่าง:**
- `p-1` = น้อย
- `p-2` = ปกติ (ปัจจุบัน)
- `p-3` = มาก
- `p-4` = มากมาก

#### แถบสถานะ
```javascript
// บรรทัด 1007
className="px-2 py-1"
```

**ปรับระยะห่าง:**
- `px-2` = ซ้าย-ขวา
- `py-1` = บน-ล่าง
- เปลี่ยนเป็น `px-3 py-2` = กว้างและสูงขึ้น

---

## ✨ เอฟเฟกต์และ Animation

### 🖱️ Hover Effect (เมื่อเมาส์ชี้)

```javascript
// บรรทัด 1000
className="hover:shadow-lg hover:brightness-95 transition-all duration-200"
```

**ปรับแต่งได้:**

#### เงา (Shadow)
- `hover:shadow-sm` = เงาเล็ก
- `hover:shadow-lg` = เงาใหญ่ (ปัจจุบัน)
- `hover:shadow-xl` = เงาใหญ่มาก
- `hover:shadow-2xl` = เงาใหญ่ที่สุด

#### ความสว่าง (Brightness)
- `hover:brightness-90` = มืดลง 10%
- `hover:brightness-95` = มืดลง 5% (ปัจจุบัน)
- `hover:brightness-105` = สว่างขึ้น 5%
- `hover:brightness-110` = สว่างขึ้น 10%

#### ความเร็วเอฟเฟกต์
- `duration-100` = เร็วมาก
- `duration-200` = เร็ว (ปัจจุบัน)
- `duration-300` = ปกติ
- `duration-500` = ช้า

### 🔄 Animation หมุน (Override Icon)

```javascript
// บรรทัด 1009
{isOverridden && <RefreshCw className="h-3 w-3 animate-spin" />}
```

**ปรับแต่งได้:**
- `animate-spin` = หมุนตลอด (ปัจจุบัน)
- `animate-pulse` = กระพริบ
- ลบ `animate-spin` = ไม่หมุน

---

## 🎯 ตัวอย่างการปรับแต่ง

### ตัวอย่างที่ 1: ทำให้ตารางใหญ่ขึ้น

```javascript
// เปลี่ยนจาก
<th className="p-2 text-sm min-w-[140px]">

// เป็น
<th className="p-4 text-base min-w-[180px]">
```

### ตัวอย่างที่ 2: เปลี่ยนสีออนไลน์เป็นม่วง

```javascript
// เปลี่ยนจาก
const statusBgColor = status === 'online' ? 'bg-blue-500' : ...

// เป็น
const statusBgColor = status === 'online' ? 'bg-purple-500' : ...
```

### ตัวอย่างที่ 3: เพิ่มเวลาถึง 8 โมงเย็น

```javascript
// เปลี่ยนจาก
['08:00', '09:00', ..., '17:00']

// เป็น
['08:00', '09:00', ..., '17:00', '18:00', '19:00', '20:00']
```

### ตัวอย่างที่ 4: ทำให้ hover เด่นชัดขึ้น

```javascript
// เปลี่ยนจาก
className="hover:shadow-lg hover:brightness-95"

// เป็น
className="hover:shadow-2xl hover:brightness-110 hover:scale-105"
```

---

## 📚 คำศัพท์ Tailwind CSS

| Class | ความหมาย | ตัวอย่าง |
|-------|----------|----------|
| `p-{n}` | Padding ทุกด้าน | `p-2`, `p-4` |
| `px-{n}` | Padding ซ้าย-ขวา | `px-3` |
| `py-{n}` | Padding บน-ล่าง | `py-2` |
| `text-{size}` | ขนาดตัวอักษร | `text-xs`, `text-lg` |
| `bg-{color}` | สีพื้นหลัง | `bg-blue-500` |
| `border` | ขอบ | `border-2` |
| `rounded-{size}` | มุมโค้ง | `rounded-lg` |
| `shadow-{size}` | เงา | `shadow-xl` |
| `hover:` | เมื่อเมาส์ชี้ | `hover:bg-blue-600` |
| `transition-all` | เอฟเฟกต์เปลี่ยนแปลง | - |
| `duration-{ms}` | ความเร็วเอฟเฟกต์ | `duration-300` |

---

## 💡 Tips

1. **ทดสอบทีละอย่าง** - เปลี่ยนแค่ 1-2 อย่างแล้วดูผล
2. **บันทึกก่อนแก้** - Copy โค้ดเดิมไว้ก่อน
3. **ใช้ Browser DevTools** - กด F12 เพื่อทดลองแก้ CSS แบบ real-time
4. **ดูเอกสาร Tailwind** - https://tailwindcss.com/docs

---

## 🆘 ถ้าเจอปัญหา

1. รีเฟรชหน้าเว็บ (F5)
2. ตรวจสอบ Console (F12) ดู error
3. ลอง Undo การเปลี่ยนแปลง
4. ถามฉันได้เลย!
