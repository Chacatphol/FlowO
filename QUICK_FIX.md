# แก้ไขปัญหา scheduleOverrides - Copy Code นี้ไปใส่

## ขั้นที่ 1: เพิ่ม logging ตอนโหลดข้อมูล

เปิดไฟล์ `src/App.jsx` หาบรรทัดที่มี:

```javascript
console.log("📥 โหลดข้อมูลจาก Firestore:", data);
```

**เพิ่มบรรทัดนี้ด้านล่าง:**

```javascript
console.log("🔄 scheduleOverrides ที่โหลด:", data.scheduleOverrides);
```

**ผลลัพธ์ควรเป็น:**

```javascript
const data = docSnap.data();
console.log("📥 โหลดข้อมูลจาก Firestore:", data);
console.log("🔄 scheduleOverrides ที่โหลด:", data.scheduleOverrides);  // ← เพิ่มบรรทัดนี้
dispatch({ type: 'load', payload: data });
```

---

## ✅ เสร็จแล้ว!

ตอนนี้ logging ครบแล้ว:
- ✅ ตอนบันทึก (ฉันแก้ไขให้แล้ว)
- ⏳ ตอนโหลด (รอคุณเพิ่มบรรทัดเดียว)

---

## 🧪 ทดสอบ

1. **Save ไฟล์**
2. **รีเฟรชเว็บ (F5)**
3. **เปิด Console (F12)**
4. **กดเปลี่ยนสถานะชั่วคราว**
5. **ดู Console** - ควรเห็น:
   ```
   💾 กำลังบันทึกข้อมูลลง Firestore...
   🔄 scheduleOverrides: { "xxx_2025-12-02": "online" }
   📦 จำนวน keys: 1
   ✅ บันทึกข้อมูลสำเร็จ!
   ```
6. **รีเฟรช (F5)**
7. **ดู Console** - ควรเห็น:
   ```
   📥 โหลดข้อมูลจาก Firestore: {...}
   🔄 scheduleOverrides ที่โหลด: { "xxx_2025-12-02": "online" }
   ```

---

## 📸 แล้วส่ง Screenshot Console ให้ฉันดู!

ส่งภาพ Console หลังจาก:
1. กดเปลี่ยนสถานะ
2. รีเฟรช

แล้วฉันจะช่วยแก้ไขต่อ!
